# Project: example
# Copy this file and rename to your project name.
# Usage: pipeline -p <name> new "bug: something broke"

# ============ REQUIRED ============

# GitHub repo (org/repo)
REPO="Obsolete-Robot/your-repo"

# Discord guild (server)
GUILD_ID="1428028127879368845"

# Discord channels
FORUM_CHANNEL=""          # Issues forum channel ID
PR_REVIEW_CHANNEL=""      # PR review channel ID
PRODUCTION_CHANNEL=""     # Production/announcements channel ID (optional)

# Webhook files (one per channel — create via Discord UI or API)
FORUM_WEBHOOK_FILE="$HOME/.config/discord/projects/example/forum-webhook"
REVIEWS_WEBHOOK_FILE="$HOME/.config/discord/projects/example/reviews-webhook"
PRODUCTION_WEBHOOK_FILE="$HOME/.config/discord/projects/example/production-webhook"

# ============ OPTIONAL OVERRIDES ============

# Forum tags (get IDs from Discord API)
TAG_BUG=""
TAG_FEATURE=""
TAG_TASK=""
TAG_RESOLVED=""

# Override agent IDs per project (defaults come from pipeline.conf)
# WORKER_ID="..."
# REVIEWER_ID="..."
